
1. LinearRegression_OneDimension:
   data_1d.csv - it is generated using for a given line equation. y = mx + c
   It calculates m and c using the formules
   It does the same using Gradient decent using dy/dm and dy/dc 

2. LinearRegression_math_model.py  - uses simple m and c definition of linear equations and finds its solution.

3. LinearRegression_sklearn_package - sovles the above problem using sklearn , LinearRegression.

4. gradient_descent - explaing using dummy cost function and its derivates for two variables.

5. Two dimention: ( Y = w1*x1 + w2*x2 ); It is a plan 
LinearRegression_TwoDimension.py - Uses to Ax=B np.linalg.solve()


